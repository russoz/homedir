#!/usr/bin/perl

sub get_version {
	my $m = shift;

	eval "require $m";
	return $@ if $@;

	return eval '$'.$m.'::VERSION';
}

printf("%-60s %s\n", 'Module', 'Version');
printf("%-60s %s\n", '=' x 60, '=' x 10 );
foreach (@ARGV) {
	printf("%-60s %s\n", $_, get_version($_));
}
